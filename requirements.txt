# The MIT License (MIT)
# Copyright (c) 2025 Jozef Darida  (LinkedIn/Xing)
# For full license text, see the LICENSE file in the project root.

# --- Core Application Libraries ---
# Used for API calls and timezone handling in the serverless (run_once.py) model.
Babel>=2.14.0
groq>=0.9.0
httpx>=0.23.0
tzdata>=2024.1

# --- Database & Auth ---
# Required for interacting with Firestore and Firebase Authentication
firebase-admin>=6.2.0

# --- Web Service Model Dependencies ---
# The libraries below are required ONLY when running the application as a long-running
# web service using main.py (e.g., on Render). For the current serverless model
# on GitHub Actions, they are not needed.
# Uncomment the two lines below if you switch back to the web service model.
# apscheduler>=3.10.4
# Flask>=3.0.0

# --- Local Development Utilities ---
# For loading .env files during local testing. Not strictly needed in production.
python-dotenv>=1.0.0

# --- Monitoring & Diagnostics ---
psutil>=5.9.0
sentry-sdk>=2.35.0

# --- External API Client Libraries ---
# Specific versions are pinned or set to a minimum for stability.
gspread==5.12.4
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client>=2.88.0
cloudinary>=1.30.0